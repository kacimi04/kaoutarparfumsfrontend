services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kaoutarparfums-frontend
    ports:
      - "4200:4200"
    environment:
      - NODE_ENV=production
      - PORT=4200
    restart: unless-stopped
